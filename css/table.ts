import { css } from "@emotion/css";

export const table_th = css`
  white-space: nowrap;
  text-align: left;
  padding: 16px;
  line-height: 20px;
  font-weight: 500;
  border-bottom: 1px solid rgba(0, 0, 0, 0.12);
`;

export const table_td = css`
  white-space: pre-wrap;
  text-align: left;
  padding: 16px;
  line-height: 20px;
  font-weight: 400;
  font-size: 14px;
  border-bottom: 1px solid rgba(0, 0, 0, 0.12);
`;
